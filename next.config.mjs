/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
      domains: ['readymadeui.com'], // Add the hostname for external images
    },
  };
  
  export default nextConfig;
  
